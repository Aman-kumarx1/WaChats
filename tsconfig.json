{
  "compilerOptions": {
    /* Basic Options */
    "target": "esnext",                      // Latest JavaScript features
    "module": "nodenext",                    // Modern Node.js module system
    "moduleResolution": "nodenext",          // Required for NodeNext
    
    /* Module Detection (THE FIX) */
    "moduleDetection": "force",              // Treats every file as a module to stop redeclaration errors
    "isolatedModules": true,                 // Ensures files can be safely transpiled independently

    /* JavaScript Support */
    "allowJs": true,                         // Allows using .js files
    "checkJs": true,                         // Provides type-checking for .js files
    "noEmit": true,                          // VS Code checks the code without creating output files

    /* Reliability & Performance */
    "strict": true,                          // Enables all strict type-checking options
    "skipLibCheck": true,                    // Skips checking .d.ts files for faster performance
    "esModuleInterop": true,                 // Fixes compatibility between CommonJS and ES Modules
    "forceConsistentCasingInFileNames": true // Prevents bugs on different operating systems
  },
  "exclude": ["node_modules", "Backups"]     // Skip large folders to keep VS Code fast
}